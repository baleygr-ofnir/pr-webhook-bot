---
services:
  pr-webhook-relay:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pr-webhook-relay
    restart: unless-stopped
    env_file:
      - .env
    security_opt:
      - no-new-privileges:true
    networks:
      - npm_proxy

networks:
  npm_proxy:
    external: true